services:
  mongo:
    image: mongo:8.0
    container_name: incomeflow-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - incomeflow_mongo_data:/data/db

volumes:
  incomeflow_mongo_data:
