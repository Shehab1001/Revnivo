# Leave blank in local development to use Vite's /api proxy.
# For production, prefer the same site (or a trusted API subdomain over HTTPS).
VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
